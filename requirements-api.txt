# Lean deps for the React + FastAPI single-service deploy (no Streamlit/Plotly).
# Keeps the build small and memory low for free hosting tiers.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3
fastapi>=0.110
uvicorn>=0.27
